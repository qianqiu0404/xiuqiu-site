name: Static Market Radar Gate and Merge

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: static-market-radar-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  gate-and-merge:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      startsWith(github.event.pull_request.head.ref, 'automation/market-radar-') &&
      github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Restrict changed files
        env:
          BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          git diff --name-only "$BASE_SHA" "$HEAD_SHA" > /tmp/market-radar-files.txt
          if grep -Ev '^(content/market-radar/[0-9]{4}-[0-9]{2}-[0-9]{2}\.md|src/data/generatedMarketRadars\.ts|src/data/generatedMarketRadarAll\.ts|src/data/generatedMarketRadarLoader\.ts|src/data/generatedMarketRadarMonths/[0-9]{4}-[0-9]{2}\.ts|public/sitemap\.xml)$' /tmp/market-radar-files.txt; then
            echo "Static market radar PR changed files outside the allowlist."
            exit 1
          fi
          test -s /tmp/market-radar-files.txt
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run test:market-radar
      - run: npm run build
      - name: Ensure generated public data is committed
        run: >-
          git diff --exit-code --
          src/data/generatedMarketRadars.ts
          src/data/generatedMarketRadarAll.ts
          src/data/generatedMarketRadarLoader.ts
          src/data/generatedMarketRadarMonths
          public/sitemap.xml
      - name: Squash merge verified static market radar
        env:
          GH_TOKEN: ${{ github.token }}
          PR_URL: ${{ github.event.pull_request.html_url }}
        run: gh pr merge "$PR_URL" --squash --delete-branch
